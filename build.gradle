import com.duwei.buildsrc.Libs
import com.duwei.buildsrc.Version
// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    apply from: rootProject.file('dependencies.gradle')
    ext.v = [
            'compose': '1.0.1',
            'composeActivity': '1.3.1',
            'composeConstraintLayout': '1.0.0-beta02',
            'composeNavigation': '2.4.0-alpha06',
    ]
    ext.deps = [
            'compose': [
                    'activityCompose'   : "androidx.activity:activity-compose:${v.composeActivity}",
                    'composeComplier'   : "androidx.compose:compose-compiler:${v.compose}",
                    'composeNavigation' : "androidx.navigation:navigation-compose:${v.composeNavigation}",
                    'composeRuntime'    : "androidx.compose.runtime:runtime:${v.compose}",
                    'constraintLayout'  : "androidx.constraintlayout:constraintlayout-compose:${v.composeConstraintLayout}",
                    'core'              : "androidx.compose.ui:ui:${v.compose}",
                    'foundation'        : "androidx.compose.foundation:foundation:${v.compose}",
                    'tooling'           : "androidx.compose.ui:ui-tooling:${v.compose}",
                    'layout'            : "androidx.compose.foundation:foundation-layout:${v.compose}",
                    'material'          : "androidx.compose.material:material:${v.compose}",
                    'savedInstanceState': "androidx.compose.runtime:runtime-saveable:${v.compose}",
                    'uiTest'            : "androidx.compose.ui:ui-test-junit4:${v.compose}",
                    'uiLiveData'        : "androidx.compose.runtime:runtime-livedata:${v.compose}"
            ],
    ]
    repositories {
        google()
        maven{ url'https://maven.aliyun.com/nexus/content/groups/public/' }
         maven{ url'https://maven.aliyun.com/nexus/content/repositories/jcenter'}
//        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.0.4'
        classpath Libs.Kotlin_Gradle_Plugin
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${Version.kotlin_version}"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        google()
//        jcenter()
        maven{ url'https://maven.aliyun.com/nexus/content/groups/public/' }
        maven{ url'https://maven.aliyun.com/nexus/content/repositories/jcenter'}
        maven { url "https://jitpack.io" }

        //融云 maven 仓库地址
        maven {url "https://maven.rongcloud.cn/repository/maven-releases/"}
    }

    //support library 强制使用指定的版本，防止被第三方库中的高版本替代
    configurations.all {
        resolutionStrategy.eachDependency { DependencyResolveDetails details ->
            def requested = details.requested
            if (requested.group == 'com.android.support') {
                if (!requested.name.startsWith("multidex")) {
                    details.useVersion '26.1.0'
                }
            }
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
